interface:
  display_name: "Agent Memory"
  short_description: "Maintain project-level agent memory for continuity and handoff."
  default_prompt: "Use $agent-memory to preserve project decisions, durable preferences, topic threads, and handoff context when this work needs continuity."
